# AssetManager 2.3.5 简要使用手册

## 1. 安装

1. 打开 `AssetManager-2.3.5.dmg`
2. 将 `AssetManager.app` 拖入 `Applications`
3. 首次打开如遇系统提示，请在系统设置中允许应用运行

## 2. 资产导入

1. 打开应用后进入“资产列表”
2. 点击“添加”或导入 CSV
3. 确认资产名称、外编号、状态、存放地等字段正确
4. 已导入来源支持“更新 CSV”，会优先按外编码对比，保留已匹配资产的当前状态
5. 如果新 CSV 里有消失条目，系统会先列出名称、外编码和当前状态，再确认是否删除

## 3. 出库 / 入库

1. 在“资产列表”中双击某条资产进入详情
2. 点击“出库”或“归还”
3. 填写操作人、备注、预计归还时间后确认
4. 资产状态会实时更新

## 4. iCloud 同步

- iCloud 当前为手动同步
- 进入设置后，可手动执行：
  - 从同步目录导入
  - 导出到同步目录
  - 双向同步

## 5. 飞书同步

1. 进入设置页的“飞书多维表格”
2. 填入 App ID、App Secret、资产表和记录表配置
3. 点击“保存配置”
4. 保存后会自动执行一次飞书同步
5. 后续本地资产状态变化会自动回写飞书
6. 全量同步会对比并更新现有条目，不是只追加新记录

当前推荐表配置：

- 资产表：`tblNjryuSQbfXpXM`
- 操作记录表：`tbluOIuGYvgcU9b0`

## 6. 常见说明

- 如果飞书记录表之前出现重复记录，建议先完成一次最新版本同步，再观察是否继续新增重复项
- 如果扫码或提醒事项不可用，请检查系统权限
- 如果同步失败，请先看设置页中的状态提示和错误信息
